/**
 * Единая шкала отступов - вместо того чтобы в одном компоненте писать
 * padding: 12, а в соседнем padding: 13 "на глазок", везде используем имена
 * отсюда (spacing.sm, spacing.md ...). Так весь интерфейс выглядит ровно,
 * и поменять "воздух" во всём приложении - это правка одного числа здесь,
 * а не десятков файлов.
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
} as const;

export const radius = {
  sm: 6,
  md: 8,
  lg: 12,
  round: 999, // для полностью круглых элементов (аватар, круглая кнопка)
} as const;
