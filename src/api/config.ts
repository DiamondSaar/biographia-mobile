/**
 * Адрес бэкенда Biographia, с которым говорит приложение.
 *
 * Прод-адрес, а не локальный dev-сервер на компьютере разработчика -
 * телефон для тестирования оказался на мобильном интернете, а не в той
 * же Wi-Fi-сети, что ноутбук (см. README.md), так что локальный IP
 * (192.168.x.x) ему в принципе недостижим. Прод же виден из любой сети.
 *
 * Если понадобится всё-таки погонять против локального бэкенда (тот же
 * Wi-Fi) - временно верните `http://<LAN-IP компьютера>:5010`, узнать
 * IP можно через `Get-NetIPAddress` в PowerShell (см. README.md, раздел 2).
 *
 * Почему это отдельный файл, а не строка внутри client.ts: один адрес
 * используется в нескольких местах, и явно видно, что именно нужно
 * поменять при переходе между прод/локальной разработкой - не искать
 * по всему коду.
 */
export const API_BASE_URL = 'https://biographia.ssod.pro';
