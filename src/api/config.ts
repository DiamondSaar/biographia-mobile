/**
 * Адрес бэкенда Biographia, с которым говорит приложение.
 *
 * Сейчас это локальный dev-сервер на компьютере разработчика (тот же Wi-Fi,
 * что и телефон - см. корневой README.md, раздел 3). Когда приложение
 * будет готово для реальных пользователей, здесь нужно будет заменить
 * значение на прод-адрес - https://biographia.ssod.pro.
 *
 * Почему это отдельный файл, а не строка внутри client.ts: один адрес
 * используется в нескольких местах (обычные запросы + загрузка файлов),
 * и явно видно, что именно нужно поменять при переходе на прод/другую
 * машину - не искать по всему коду.
 */
export const API_BASE_URL = 'http://192.168.1.43:5010';
